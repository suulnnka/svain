#include <stdio.h>

int main(int argc, char *argv[])
{
	if(argc<2){
		complie("test.pl");
	}else{
		int i;
		for(i = 1; i<= argc;i++){
			char *str = get_src(argv[i]);
			Token_List *list = lex(str);
			AST *root = yacc(list);

		}
	}

	return 0;
}
